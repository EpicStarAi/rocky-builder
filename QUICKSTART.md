# 🚀 ШВИДКИЙ СТАРТ ROCKY BUILDER

## Що вже готово

✅ Повна структура проекту  
✅ Docker Compose для всієї інфраструктури  
✅ PostgreSQL база з міграціями  
✅ Next.js frontend з ROCKY BUILDER брендингом  
✅ Мультимовність UA/RU  
✅ Header та Footer компоненти  
✅ Головна сторінка з hero-секцією  
✅ n8n воркфлоу для парсингу domfasad.com.ua  

## Запуск за 5 хвилин

### 1. Встановіть залежності

```bash
cd frontend
npm install
cd ..
```

### 2. Створіть .env файл

```bash
cp .env.example .env
```

### 3. Запустіть інфраструктуру

```bash
docker-compose up -d
```

Чекайте ~30 секунд поки все запуститься.

### 4. Перевірте сервіси

```bash
docker-compose ps
```

Всі сервіси мають бути "Up":
- rocky-postgres (PostgreSQL)
- rocky-redis (Redis)
- rocky-meilisearch (Пошук)
- rocky-n8n (Автоматизація)

### 5. Запустіть frontend

```bash
cd frontend
npm run dev
```

Відкрийте: **http://localhost:3000/ua**

## 🎨 Що побачите

- ✨ Красиву головну з ROCKY BUILDER брендингом
- 🌐 Перемикач мов UA/RU
- 📱 Мобільну версію
- 🎯 Header з навігацією
- 📄 Footer з контактами

## 🔧 Наступні кроки

### Опція 1: Тестування парсингу

```bash
# Відкрийте n8n
open http://localhost:5678

# Логін: admin
# Пароль: rocky2024

# Імпортуйте воркфлоу:
# Workflows → Import from File → n8n-workflows/domfasad-scraper.json

# Запустіть воркфлоу (Execute Workflow)
```

### Опція 2: Додавання тестових товарів

```bash
# Підключіться до PostgreSQL
docker exec -it rocky-postgres psql -U rocky -d rocky_builder

# Додайте тестові категорії
INSERT INTO categories (slug, name_ua, name_ru) VALUES 
  ('pokrivlya', 'Покрівля', 'Кровля'),
  ('fasad', 'Фасад', 'Фасад');

# Додайте тестовий товар
INSERT INTO products (slug, name_ua, name_ru, description_ua, base_price, category_id, in_stock, is_active)
VALUES (
  'metalocherepitsya-test',
  'Металочерепиця тестова',
  'Металлочерепица тестовая',
  'Опис тестового товару',
  250.00,
  (SELECT id FROM categories WHERE slug = 'pokrivlya'),
  true,
  true
);
```

### Опція 3: Робота над дизайном

Файли для редагування:
- `frontend/src/app/[locale]/page.tsx` — Головна сторінка
- `frontend/src/components/layout/Header.tsx` — Шапка
- `frontend/src/components/layout/Footer.tsx` — Футер
- `frontend/tailwind.config.js` — Кольори та стилі

## 📂 Ключові файли

| Файл | Призначення |
|------|-------------|
| `docker-compose.yml` | Вся інфраструктура |
| `backend/init.sql` | База даних |
| `frontend/src/app/[locale]/page.tsx` | Головна сторінка |
| `frontend/messages/ua.json` | Українські переклади |
| `n8n-workflows/` | Воркфлоу для парсингу |

## ❓ Проблеми?

### PostgreSQL не запускається
```bash
docker-compose down
docker volume prune
docker-compose up -d postgres
```

### Frontend не компілюється
```bash
cd frontend
rm -rf .next node_modules
npm install
npm run dev
```

### Порт зайнятий
Змініть порти в `docker-compose.yml`:
```yaml
ports:
  - "3001:3000"  # Frontend
  - "5433:5432"  # PostgreSQL
```

## 🎯 Що робити далі?

1. **Для демо замовнику:**
   - Додайте реальні зображення в `frontend/public/images/`
   - Налаштуйте кольори в `tailwind.config.js`
   - Додайте кілька тестових товарів через PostgreSQL

2. **Для розробки:**
   - Встановіть Medusa.js backend
   - Створіть API для каталогу
   - Додайте сторінку products/[slug]

3. **Для production:**
   - Deploy frontend на Vercel
   - Setup Hetzner VPS для backend
   - Налаштуйте домен

## 📞 Підтримка

Якщо щось не працює — напишіть мені, розберемося! 🚀
